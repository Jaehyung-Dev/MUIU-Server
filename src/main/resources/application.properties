spring.application.name=${spring.application.name}

server.port=${server.port}

spring.datasource.driver-class-name=${spring.application.name}
spring.datasource.url=${spring.datasource.url}
spring.datasource.username=${spring.datasource.username}
spring.datasource.password=${spring.datasource.password}

spring.jpa.database=${spring.jpa.database}
spring.jpa.database-platform=${spring.jpa.database-platform}
spring.jpa.hibernate.ddl-auto=${spring.jpa.hibernate.ddl-auto=}
spring.jpa.show-sql=${spring.jpa.show-sql}
spring.jpa.properties.hibernate.format_sql=${spring.jpa.properties.hibernate.format_sql}

sms.api-key=${sms.api-key}
sms.api-secret-key=${sms.api-secret-key}
sms.domain=${sms.domain}
sms.from-number=${sms.from-number}

# GMail Settings
spring.mail.host=${spring.mail.host}
spring.mail.port=${spring.mail.port}
spring.mail.username=${spring.mail.username}
spring.mail.password=${spring.mail.password}
spring.mail.properties.mail.smtp.auth=${spring.mail.properties.mail.smtp.auth}
spring.mail.properties.mail.smtp.starttls.enable=${spring.mail.properties.mail.smtp.starttls.enable}

naver.clientId=${naver.clientId}
naver.secretKey=${naver.secretKey}

# naver cloud API
ncp.accessKey=${ncp.accessKey}
ncp.secretKey=${ncp.secretKey}
ncp.regionName=${ncp.regionName}
ncp.endPoint=${ncp.endPoint}

#filesize
spring.servlet.multipart.max-file-size=${spring.servlet.multipart.max-file-size}
spring.servlet.multipart.max-request-size=${spring.servlet.multipart.max-request-size}

# Clova API Configuration
clova.host=${clova.host}
clova.api_key=${clova.api_key}
clova.api_key_primary_val=${clova.api_key_primary_val}
clova.request_id=${clova.request_id}

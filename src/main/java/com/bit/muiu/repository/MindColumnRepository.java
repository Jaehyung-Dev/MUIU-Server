package com.bit.muiu.repository;

import com.bit.muiu.entity.MindColumn;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MindColumnRepository extends JpaRepository<MindColumn, Long> {
}
